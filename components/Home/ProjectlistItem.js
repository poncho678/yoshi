import React from "react";
import styles from "./ProjectlistItem.module.css";

function ProjectlistItem({ project }) {
  return <article className={styles.project}>Projectlistitem</article>;
}

export default ProjectlistItem;
